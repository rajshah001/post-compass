// Placeholder content script. Reserved for future features like one-click posting.
(() => {
  // Keep lightweight to avoid impacting page performance.
  // Example: expose a simple API for future use.
  window.postCompass = {
    version: '0.1.0'
  };
})();


